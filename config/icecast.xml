<?xml version="1.0"?>
<!-- NAVO RADIO — Icecast config. Port 8001 (backend=8000). Mount /live. -->
<icecast>
    <location>NAVO RADIO</location>
    <admin>navoradio@localhost</admin>
    <!-- 127.0.0.1 убирает предупреждение «hostname not configured» при локальной разработке -->
    <hostname>127.0.0.1</hostname>

    <limits>
        <clients>100</clients>
        <sources>2</sources>
        <queue-size>524288</queue-size>
        <client-timeout>30</client-timeout>
        <header-timeout>15</header-timeout>
        <source-timeout>10</source-timeout>
        <burst-size>65535</burst-size>
    </limits>

    <authentication>
        <!-- Задайте пароли в .env: ICECAST_SOURCE_PASSWORD, ICECAST_ADMIN_PASSWORD. Для локального icecast — скопируйте значения сюда вручную. -->
        <source-password>CHANGE_ME</source-password>
        <relay-password>CHANGE_ME</relay-password>
        <admin-user>admin</admin-user>
        <admin-password>CHANGE_ME</admin-password>
    </authentication>

    <listen-socket>
        <port>8001</port>
        <bind-address>127.0.0.1</bind-address>
    </listen-socket>

    <http-headers>
        <header name="Access-Control-Allow-Origin" value="*" />
    </http-headers>

    <paths>
        <!-- Логи в папку проекта. Путь относительно CWD при запуске (корень проекта). -->
        <logdir>dev/icecast_logs</logdir>
        <webroot>C:/Program Files/Icecast/web</webroot>
        <adminroot>C:/Program Files/Icecast/admin</adminroot>
        <alias source="/" destination="/status.xsl"/>
    </paths>

    <logging>
        <accesslog>access.log</accesslog>
        <errorlog>error.log</errorlog>
        <loglevel>warning</loglevel>
    </logging>

    <security>
        <prng-seed type="static">NAVO-RADIO-icecast-2024-prng-seed-min-64-chars-required-for-icecast</prng-seed>
    </security>
</icecast>
